# Généré automatiquement dans le dossier racine des données (ex. C:\SchoolMatrix\.env.prod)
# Pour production locale Docker — ne pas modifier les lignes DB_HOST et DB_PORT ci-dessous.

DB_HOST=db
DB_PORT=5432
DB_USER=schoolmatrix
DB_PASS=schoolmatrix
DB_NAME=schoolmatrix

JWT_SECRET=change-me-in-production

# Optionnel
# NODE_ID=LOCAL
# AWS_REGION=eu-west-1
# AWS_S3_BUCKET=drive-mike
# AWS_S3_PREFIX=schoolmatrix
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
