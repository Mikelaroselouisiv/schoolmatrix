# Database (même base que Docker : docker compose up -d pour lancer uniquement la base)
DB_HOST=localhost
DB_PORT=5432
DB_USER=schoolmatrix
DB_PASS=schoolmatrix
DB_NAME=schoolmatrix

# JWT (required)
JWT_SECRET=your-secret-key-change-in-production

# NODE_ENV=development uses .env.dev
