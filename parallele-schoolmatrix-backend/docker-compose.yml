# La base PostgreSQL est dans le docker-compose à la racine du projet.
# Dev  : à la racine, lancer  docker compose up -d           (uniquement la base, port 5432)
# Prod : à la racine, lancer  docker compose --profile prod up -d
# Ce fichier ne définit plus de service pour éviter toute confusion.
