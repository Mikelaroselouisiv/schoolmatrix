# Tout est à la racine du projet.
# Base (dev) : schoolmatrix-db-dev (port 5433) — démarrer avec  docker start schoolmatrix-db-dev
# Build + run (api + web, même base) : à la racine  docker compose up -d --build   ou  .\run.ps1
